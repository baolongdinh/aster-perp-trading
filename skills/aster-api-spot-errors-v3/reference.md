# Spot Errors v3 – Reference

Error code shape: code (int), msg (string). Retry-After header on 429/418. rateLimitType: REQUEST_WEIGHT (per IP), ORDERS (per account). See api-docs for full code list.
